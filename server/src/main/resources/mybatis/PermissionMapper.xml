<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.dsc.jk.mapper.PermissionMapper">

    <!-- Base Result Map -->
    <resultMap id="baseResultMap" type="cn.dsc.jk.entity.PermissionEntity">
        <id column="permission_id" property="permissionId" jdbcType="BIGINT"/>
        <result column="permission_name" property="permissionName" jdbcType="VARCHAR"/>
        <result column="permission_code" property="permissionCode" jdbcType="VARCHAR"/>
        <result column="created_user_id" property="createdUserId" jdbcType="BIGINT"/>
        <result column="updated_user_id" property="updatedUserId" jdbcType="BIGINT"/>
        <result column="created_time" property="createdTime" jdbcType="TIMESTAMP"/>
        <result column="updated_time" property="updatedTime" jdbcType="TIMESTAMP"/>
    </resultMap>

    <!-- Base Column List -->
    <sql id="Base_Column_List">
        permission_id,
        permission_name,
        permission_code,
        created_user_id,
        updated_user_id,
        created_time,
        updated_time
    </sql>

    <!-- Select list with conditions -->
    <select id="selectList" resultMap="baseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM permission
        <where>
            <if test="permissionName != null and permissionName != ''">
                AND permission_name LIKE CONCAT('%', #{permissionName}, '%')
            </if>
            <if test="permissionCode != null and permissionCode != ''">
                AND permission_code LIKE CONCAT('%', #{permissionCode}, '%')
            </if>
        </where>
        ORDER BY created_time DESC
    </select>

</mapper>
